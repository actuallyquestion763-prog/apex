node_modules/

# Env files: ignore every .env* variant except the checked-in placeholder
# template. Order matters — the negation must come after the broad pattern.
.env
.env.*
!.env.example

dist/
build.log
*.log
*.tsbuildinfo

# Manual-deploy packaging scratch (superseded by
# .github/workflows/deploy-namecheap.yml, which builds and uploads directly)
site-upload.zip

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
